#+title: En ℝ, |a| = |a - b + b|
#+subtitle: Publicado el 31 de enero de 2024 por [[https://jaalonso.github.io][José A. Alonso]]
#+SETUPFILE: ../plantillas/nivel_1.org

* Enunciado

Demostrar con Lean4 que en \(ℝ\), \(|a| = |a - b + b|\).

Para ello, completar la siguiente teoría de Lean4:

#+begin_src lean
import Mathlib.Data.Real.Basic
variable (a b : ℝ)

example
  : |a| = |a - b + b| :=
by sorry
#+end_src

* Demostraciones con Lean4

#+begin_src lean
import Mathlib.Data.Real.Basic
variable (a b : ℝ)

-- 1ª demostración
-- ===============

example
  : |a| = |a - b + b| :=
by
  congr
  -- a = a - b + b
  ring

-- Comentario: La táctica congr sustituye una conclusión de la forma
-- A = B por las igualdades de sus subtérminos que no no iguales por
-- definición. Por ejemplo, sustituye la conclusión (x * f y = g w * f z)
-- por las conclusiones (x = g w) y (y = z).

-- 2ª demostración
-- ===============

example
  (a b : ℝ)
  : |a| = |a - b + b| :=
by { congr ; ring }

-- 3ª demostración
-- ===============

example
  (a b : ℝ)
  : |a| = |a - b + b| :=
by ring_nf
#+end_src

Se puede interactuar con las demostraciones anteriores en
[[https://live.lean-lang.org/#url=https://raw.githubusercontent.com/jaalonso/Calculemus2/main/src/Demostracion_por_congruencia.lean][Lean 4 Web]].

* Referencias

+ J. Avigad y P. Massot. [[https://bit.ly/3U4UjBk][Mathematics in Lean]], p. 41.
